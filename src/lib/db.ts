/**
 * @deprecated Use `@/lib/db/client` (Drizzle) and the repository layer instead.
 * This file is kept only to avoid breaking any overlooked imports — delete it
 * once you've confirmed no remaining usages.
 */
export {};
